from sbcapi.utils import *

# node_url = "http://localhost:5555"
# chain = get_block_chain_from_peer(node_url)
# last_block = get_last_block(node_url)
# block_by_index = get_block_by_index(node_url, last_block.index)
# block_by_hash = get_block_by_hash(node_url, last_block.miner_hash)
# block_range = get_blocks_range(node_url, 0, 2)
# print("Gimme beer")


CryptoUtils.create_mnemonic_key("umpalumpa")